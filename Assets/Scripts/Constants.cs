using System.IO;
using UnityEngine;

public static class Constants{

    public static readonly string MODELS_TEMPORARY_FOLDER_PATH = Path.Combine(Application.temporaryCachePath, "modelsTemporaryFiles");
}

